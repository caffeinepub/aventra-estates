{
  "frontend/src/components/PropertyListingFilters.tsx": "Sidebar filter panel with Listing Type radio group (All/For Sale/For Rent), location search, property type select, BHK type select, price range slider, and property flag checkboxes.",
  "frontend/src/pages/AdminUsersPage.tsx": "Admin-only page that lists all registered users in a table with their name, contact info, principal ID, and role, with the ability to promote/demote users to/from admin role.",
  "frontend/src/pages/AdminApprovalsPage.tsx": "Admin-only page that displays all user approval requests with their current status (pending/approved/rejected) and action buttons to approve or reject each user.",
  "frontend/src/components/Footer.tsx": "Footer component with Aventra Estates branding, quick links, property type links, and official contact info (both phone numbers as tel: links, email as mailto:, and office address). Includes caffeine.ai attribution with UTM tracking.",
  "frontend/src/components/PropertyCard.tsx": "Property card component using `images[0].getDirectURL()` for the photo; fixed navigation to use the correct route `/properties/$id` with param `id`, and added `onWishlistToggle` back to the props interface.",
  "frontend/src/components/AgentCard.tsx": "Agent/contact card showing property owner details with fallback to official Aventra Estates contact info (both phone numbers, email, WhatsApp) when no owner profile is available.",
  "frontend/src/pages/PropertyListingPage.tsx": "Property listing page that reads listingType from URL query params to pre-filter results, with client-side filtering by listing type, location, property type, BHK, price range, and flags, plus sort options.",
  "frontend/src/pages/PostPropertyPage.tsx": "Multi-step post property form (Basic Info → Property Details → Features & Photos → Review) with JPEG file upload (replacing photo URL inputs), thumbnail previews with remove buttons, and ExternalBlob conversion on submit.",
  "frontend/src/pages/PropertyDetailPage.tsx": "Property detail page with ExternalBlob gallery; fixed route param from `/property/$propertyId` to `/properties/$id`, and passed `property?.owner ?? null` to `useGetUserProfile` to satisfy `Principal | null` type.",
  "frontend/src/components/SearchBar.tsx": "Search bar with Buy/Rent tabs that navigate to the property listing page with listingType=rent or listingType=sale query parameters, plus location-based search.",
  "frontend/src/components/FeaturedProperties.tsx": "Section component displaying up to 6 featured properties in a responsive grid, with loading skeletons and an empty state, using PropertyCard without the removed `view` prop.",
  "frontend/src/pages/WishlistPage.tsx": "Wishlist page showing saved properties for authenticated users, using PropertyCard with `showWishlist={true}` instead of the removed `view` prop, with remove-from-wishlist functionality.",
  "frontend/src/pages/EditPropertyPage.tsx": "Edit property form with JPEG file upload; fixed route param from `/edit-property/$propertyId` to `/edit-property/$id` and corrected `updateProperty.mutateAsync` call shape to use `{ id, update }`.",
  "frontend/src/components/ImageGallerySlider.tsx": "Image gallery slider component that accepts ExternalBlob[] and renders main image with prev/next navigation and thumbnail strip, with a placeholder shown when no images exist.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; fixed `photos` type from `string[]` to `Uint8Array[]` in `useCreateProperty` and `useUpdateProperty`, and updated `useGetUserProfile` to accept `Principal | null | undefined`.",
  "frontend/src/pages/UserDashboard.tsx": "User dashboard showing property listings; fixed thumbnail to use only `property.images[0].getDirectURL()` instead of `property.photos[0]` (which is now `Uint8Array`, not a displayable string)."
}